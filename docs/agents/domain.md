# Domain docs

## Layout

Single-context repo — one `CONTEXT.md` + `docs/adr/` at the repo root.

## Consumer rules

- `CONTEXT.md` describes the project's domain language and glossary.
- `docs/adr/` contains architectural decision records.
- Skills should read these before proposing structural changes.

## Current state

- No `CONTEXT.md` exists yet.
- No `docs/adr/` directory exists yet.
- Domain knowledge is scattered in `docs/superpowers/specs/` and `docs/superpowers/plans/`.
